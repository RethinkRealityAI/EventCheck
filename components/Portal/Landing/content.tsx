/**
 * @deprecated Import from `../content/landingDefaults` instead.
 * Kept only so any stale path resolves during the CMS migration.
 */
export { LANDING_DEFAULTS, PORTAL_DEFAULTS } from '../content/landingDefaults';
